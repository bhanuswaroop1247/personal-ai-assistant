# Rename this file to secrets.toml and fill in your values.
# secrets.toml is gitignored — never commit it.
#
# On Streamlit Cloud, add these via the app's Secrets settings panel instead.

GEMINI_API_KEY = "your-gemini-api-key-here"
PARA_ROOT_PATH = "/mount/src/your-repo/sample_para"
